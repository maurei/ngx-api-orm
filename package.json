{
  "name": "ngx-api-orm",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "compodoc": "compodoc --disableInternal -p tsconfig.json -s --includes core/guides --includesName \"Usage guides\" --theme material --hideGenerator --output ./docs/",
    "build": "cd ./core && npm run pack:lib && cd ../json-api && npm install --save ../core.tgz && npm run pack:lib"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@compodoc/compodoc": "^1.1.5"
  },
  "devDependencies": {
    "reflect-metadata": "^0.1.13"
  }
}
